import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    notifications: {
        flex: 1,
        backgroundColor: "#fff",
        padding: 10
    }
})

export default styles